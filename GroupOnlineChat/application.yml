server:
  port: 8080

spring:
  datasource:
    username: sergey
    password: cthsq30896
    url: jdbc:mysql://localhost:3306/chat
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create
    show-sql: false